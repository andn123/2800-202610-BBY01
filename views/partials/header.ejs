<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title><%= title %></title>

    <% cssFiles.forEach(function(file) { %>
    <link rel="stylesheet" href="/css/<%= file %>">
    <% }); %>
</head>

<body>